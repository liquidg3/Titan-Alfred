{
    "description": "This schema will apply to all adapters. Each adapter may mixin its own.",
    "properties":    {
        "port": {
            "type":    "integer",
            "options": {
                "label":   "Port",
                "default": 8080
            }
        }
    }
}