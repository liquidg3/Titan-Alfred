{
    "description": "Settings for Alfred",
    "properties": {
        "autostart": {
            "type":    "boolean",
            "options": {
                "label":       "Autostart",
                "description": "Should I launch a web server on load?"
            }
        },

        "defaultStrategy": {
            "type": "select",
            "options": {
                "label":  "Webserver"
            }
        }
    }
}